{
  "frontend/src/components/LandingPage.tsx": "Animated slideshow landing page for unauthenticated users with 4 car slides, auto-advance, slide indicators, and a login button that triggers Internet Identity authentication.",
  "frontend/src/components/AuthGate.tsx": "Authentication gate that handles all auth states (loading, unauthenticated, authenticated without profile, authenticated with profile) — shows a loading spinner during initialization, LandingPage for unauthenticated users, an inline ProfileSetupForm for new users, and renders children once authenticated with a profile.",
  "frontend/src/components/Layout.tsx": "Authenticated app shell with TopBar, a horizontal sponsor banner row (eBay, BullBoost, Enjuku Racing), a main content Outlet wrapped in React.Suspense, and BottomNav.",
  "frontend/index.html": "HTML entry point with SEO metadata, PWA manifest link, Apple touch icon meta tags, and an inline service worker registration script at the end of the body.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — covers user profiles, posts, comments, likes, follows, messages, events, builds, marketplace listings, search, leaderboard, and AI assistant, with proper error handling and actor-readiness checks throughout.",
  "frontend/src/pages/ProfilePage.tsx": "Profile page showing user avatar, cover photo, bio, stats, and tabbed posts/reels/builds content; follow/unfollow and message buttons check guest status, and post deletion is handled internally by PostCard without an onDeleteRequest prop.",
  "frontend/src/pages/MessagesPage.tsx": "Messages list page that now fetches the current user's followers and following lists and only displays users with at least a one-way follow relationship as available chat partners, with a \"Followers only\" lock indicator and an empty state prompting users to follow others.",
  "frontend/src/pages/ChatThreadPage.tsx": "Chat thread page that now checks if a follow relationship exists (either direction) before showing the message thread. If no relationship exists, renders a locked/restricted state with a clear message and a Follow button styled in amber; existing conversations between connected users continue to work normally.",
  "frontend/src/components/PostCard.tsx": "Post card component with like/comment actions that check guest status via useGuestCheck, showing prompts for unauthenticated users; owner can delete their own posts. No longer accepts onDeleteRequest prop — deletion is handled internally.",
  "frontend/src/pages/ReelsPage.tsx": "Vertical snap-scroll reels page that re-fetches from the backend on mount — fixed the `CommentsSheet` prop from `onOpenChange` to the correct `onClose` callback, and uses `currentProfile.username` instead of the principal for owner comparison.",
  "frontend/src/pages/ReelsSearchPage.tsx": "New dedicated Reels Search page with a debounced search input, 8 category pill filters (Street, Stance, JDM, Drift, Build, Show, Track, Import), a two-column results grid showing reel cards with thumbnails, category badges, author avatars, and captions, plus skeleton loaders and empty/idle states.",
  "frontend/src/App.tsx": "Main app router with all routes registered using TanStack Router, wrapped in ErrorBoundary, QueryClientProvider, ThemeProvider, and Toaster — all page components are lazy-loaded with React.Suspense fallbacks, and route errors are caught with a visible error UI.",
  "frontend/src/pages/DiscoverPage.tsx": "Discover page with user/content search and tabbed grid, now featuring two side-by-side shortcut buttons below the search bar — one for Reels Search and a new one for the Marketplace — both navigating to their respective pages.",
  "frontend/src/components/EditProfileModal.tsx": "Edit profile modal with a circular avatar preview section at the top showing the current or newly selected profile picture, a hidden file input (accept=\"image/*\") triggered by clicking the avatar or a text button, 10 MB file size validation with an inline error message (\"File too large — please select a file under 10 MB\"), base64 encoding of valid selected images, and calls to both `useUpdateProfilePic` and `useSaveCallerUserProfile` on save with a save-error display and proper loading/disabled states.",
  "frontend/src/components/CommentsSheet.tsx": "Bottom sheet for post comments updated to use `ClickableUsername` for each comment's author name, replacing the plain `<span>` with the clickable component.",
  "frontend/src/components/UserListItem.tsx": "User list item component updated to use `ClickableUsername` for the display name, converting the outer `<button>` to a `<div>` with onClick to avoid nested interactive element issues.",
  "frontend/src/pages/HomePage.tsx": "Home feed page using `useInfiniteFeed` with `PostType.feed` argument (fixing the missing argument error), infinite scroll via IntersectionObserver, loading skeletons, error state with retry, and empty state message.",
  "frontend/src/pages/CreateReelPage.tsx": "Create Reel page with video/image upload (10 MB limit), category selector, caption, and tags — surfaces backend errors via both toast and inline display, and navigates to /reels on success.",
  "frontend/src/pages/CreateFeedPostPage.tsx": "Create Feed Post page with optional photo upload (10 MB limit), caption, and tags — surfaces backend errors via both toast notifications and inline error display, and navigates to home on success.",
  "frontend/src/pages/CreateBuildPage.tsx": "Create Build Showcase page now includes an optional photo upload section with a 10 MB file size limit per image, inline error display, image preview with clear button, and proper error handling on submission.",
  "frontend/src/pages/CreateMechanicQuestionPage.tsx": "Create Mechanic Question page with question text, optional photo upload (10 MB limit), and tags — surfaces backend errors via both toast and inline display, and navigates to /mechanics on success.",
  "frontend/src/components/BottomNav.tsx": "Bottom navigation bar with six tabs — Home, Reels, Discover, Create (floating center button), Racers (Trophy icon linking to /leaderboard), and Profile — replacing the previous Events tab with the leaderboard entry.",
  "frontend/src/pages/MarketplacePage.tsx": "Marketplace page with category filters and a 2-column listing grid; the Create/Sell button checks guest status via useGuestCheck and shows a sign-in prompt for unauthenticated users. Uses stopPropagation via a wrapper div instead of a prop on ClickableUsername.",
  "frontend/src/pages/CreateListingPage.tsx": "Create Listing page with image upload now enforcing a 10 MB size limit (increased from 1.5 MB), updated hint text, inline error display, and proper error handling on submission.",
  "frontend/src/components/CreatePostSheet.tsx": "Bottom sheet for content creation with five options (Feed Post, Reel, Build Showcase, Mechanic Question, and the new \"List for Sale\" option with amber styling that navigates to `/marketplace/create`).",
  "frontend/src/pages/AboutPage.tsx": "About RevGrid page with hero, platform description, features list, closing quote, a new \"Get the App\" install section linking to /install, support/donation section, and contact section.",
  "frontend/src/components/TopBar.tsx": "Top navigation bar updated to include a Download icon button linking to `/install` (with active amber highlight when on that route), alongside all existing navigation icons and title mappings including the new `/install` → `INSTALL APP` page title entry.",
  "frontend/src/pages/TunerShopPage.tsx": "Static Altered Imports tuner shop page with a hero banner image, about/mission section, 7 service cards with icons, closing paragraph, prominent tagline, and location/contact cards (Instagram link and tappable phone number), all styled with the dark asphalt/amber automotive theme.",
  "frontend/src/hooks/useLandingMusic.ts": "Custom React hook managing landing page background music — now starts unmuted by default with a fade-in on playback, falls back to muted autoplay if the browser blocks unmuted autoplay, and exposes `isMuted`, `isPlaying`, `toggleMute`, `startPlayback`, and `fadeOutAndStop` controls.",
  "frontend/public/sitemap.xml": "Static XML sitemap conforming to the sitemaps.org schema, listing all 9 publicly accessible RevGrid routes (home, feed, reels, discover, events, marketplace, about, tuner-shop, reels-search) with weekly changefreq and appropriate priority values.",
  "frontend/src/pages/StreetTubePage.tsx": "Fully upgraded self-contained StreetTube page with 10 new features: dark/light theme toggle with localStorage persistence, video comments system with avatar initials and timestamps, channel profile modal with subscribe toggle and video grid, share button that copies a `?v=<videoId>` URL with toast notification, auto-open video from URL query param, Watch Later save/unsave with sidebar entry, Trending view sorted by view count, search results view with horizontal list cards and tag support, notification bell with unread badge and dropdown, video edit/delete for owners with three-dot menu, and enhanced upload flow with thumbnail frame picker slider and comma-separated tags stored as pill badges.",
  "frontend/src/pages/LeaderboardPage.tsx": "Race leaderboard page updated so both podium cards and ranked list rows use `ClickableUsername` for user names, making every name on the leaderboard clickable and navigable to the user's profile.",
  "frontend/src/pages/EventsPage.tsx": "Events listing page with proper loading skeletons, error alert, and empty state — uses the fixed `useGetAllEvents` hook that correctly waits for actor initialization before fetching, preventing blank screens after login.",
  "frontend/src/pages/EventDetailPage.tsx": "Event detail page with loading skeleton, error alert, and not-found state — uses the fixed `useGetEvent` hook with actor-readiness awareness, preventing infinite loading or blank screens when navigating from the Events list.",
  "frontend/src/pages/MechanicThreadPage.tsx": "Mechanic Q&A thread page with an AIAnswerCard that now correctly imports and uses `useAskAutomotiveAssistant` (renamed from the non-existent `useAskMechanicAI`), community answers list, and an answer input form.",
  "frontend/src/pages/MechanicsPage.tsx": "Mechanics listing page with AI-powered banner, sorted question list, and error/loading states — fixed the navigation route from the invalid `/create-mechanic-question` to the correct `/mechanics/create`.",
  "frontend/src/index.css": "Global styles with OKLCH dark/light color tokens, Bebas Neue heading font, and `.ai-answer-content` component styles — fixed the build error by replacing unsupported `text-foreground/90` and `text-foreground/85` opacity modifier classes in `@apply` with plain `text-foreground` plus native CSS `opacity` properties.",
  "frontend/src/components/ClickableUsername.tsx": "Reusable inline component that renders a user's display name as a clickable button navigating to their profile page, with optional @ prefix via showAt prop. Exports the ClickableUsernameProps interface for use by other components.",
  "frontend/src/pages/BuildDetailPage.tsx": "Build detail page showing image gallery, build info with author, specs, and a comments section; guest users see a \"Sign in to comment\" prompt instead of the input form, and all ClickableUsername usages now include the required displayName prop.",
  "frontend/public/manifest.json": "Web app manifest for RevGrid PWA with name, short_name, description, amber theme_color, dark background_color, standalone display mode, start_url, and 192x192/512x512 icon references.",
  "frontend/public/sw.js": "Service worker that caches the RevGrid app shell on install, cleans up old caches on activate, and serves cached assets with a cache-first strategy while skipping ICP/canister API calls.",
  "frontend/src/pages/InstallPage.tsx": "PWA install page at /install showing step-by-step Android Chrome installation instructions, a one-tap install button when the beforeinstallprompt event is available, an already-installed state, iOS Safari instructions, and a benefits list.",
  "frontend/src/components/ProfileSetupForm.tsx": "Standalone profile setup form component for new authenticated users, handling username/display name/bio/car info input with validation and backend submission via createUser and saveCallerUserProfile."
}