{
  "frontend/src/components/LandingPage.tsx": "Animated flash intro landing page with background music integration — fixes the login button by calling `onIntroComplete?.()` immediately after a successful `login()` call in `handleLogin`, ensuring AuthGate transitions to the authenticated app even though `isAuthenticated` is passed as `false` during the intro phase.",
  "frontend/src/components/AuthGate.tsx": "Authentication gate that always shows the flash intro slideshow first on every app load by passing `isAuthenticated={false}` to `LandingPage` during the intro phase, preventing any auto-forward bypass; after the intro completes, authenticated users proceed directly to the app while unauthenticated users see the login screen via `skipIntroImmediately`.",
  "frontend/src/components/Layout.tsx": "Authenticated app shell with TopBar, a single horizontal sponsor banner row displaying all three banners (eBay image banner, BullBoost logo, Enjuku Racing) side-by-side with equal flex columns, main content Outlet, and BottomNav — all banners are individually clickable and open in new tabs on every authenticated page.",
  "frontend/index.html": "HTML entry point with comprehensive SEO metadata including a Google site verification meta tag (`ajqPDh5gAwTpW4GTSstghaOCXr99GgBnovTx1Vckcag`), descriptive title, meta description, keywords, robots directive, theme-color, Open Graph tags, and Twitter Card tags.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions, now including a new `useInfiniteFeed` hook that uses `useInfiniteQuery` to fetch all posts once, sort them newest-first, and paginate them client-side in pages of 20; `useCreatePost` and `useDeletePost` also invalidate the `infiniteFeed` query key.",
  "frontend/src/pages/ProfilePage.tsx": "Profile page with all existing tabs and actions, now including a \"StreetTube\" navigation button (red PlayCircle icon) visible only on the current user's own profile that navigates to `/streettube`.",
  "frontend/src/pages/MessagesPage.tsx": "Messages list page that now fetches the current user's followers and following lists and only displays users with at least a one-way follow relationship as available chat partners, with a \"Followers only\" lock indicator and an empty state prompting users to follow others.",
  "frontend/src/pages/ChatThreadPage.tsx": "Chat thread page that now checks if a follow relationship exists (either direction) before showing the message thread. If no relationship exists, renders a locked/restricted state with a clear message and a Follow button styled in amber; existing conversations between connected users continue to work normally.",
  "frontend/src/components/PostCard.tsx": "Post card component that fetches the author's user data via `useGetUser` and renders their `profilePicData` as a base64 data URL avatar (with default fallback), alongside existing like, comment, and delete functionality.",
  "frontend/src/pages/ReelsPage.tsx": "Vertical snap-scroll reels page where videos now default to unmuted (`muted=false`) and attempt to play with audio when active, gracefully falling back to muted if the browser blocks unmuted autoplay; the mute/unmute toggle button remains functional, and all other reel features (likes, comments, delete, author info, overlays) are preserved.",
  "frontend/src/pages/ReelsSearchPage.tsx": "New dedicated Reels Search page with a debounced search input, 8 category pill filters (Street, Stance, JDM, Drift, Build, Show, Track, Import), a two-column results grid showing reel cards with thumbnails, category badges, author avatars, and captions, plus skeleton loaders and empty/idle states.",
  "frontend/src/App.tsx": "Main app router with all routes registered including the new `/streettube` route rendering StreetTubePage, wrapped in ErrorBoundary and ThemeProvider.",
  "frontend/src/pages/DiscoverPage.tsx": "Discover page with user/content search and tabbed grid, now featuring two side-by-side shortcut buttons below the search bar — one for Reels Search and a new one for the Marketplace — both navigating to their respective pages.",
  "frontend/src/components/EditProfileModal.tsx": "Edit profile modal with a circular avatar preview section at the top showing the current or newly selected profile picture, a hidden file input (accept=\"image/*\") triggered by clicking the avatar or a text button, 10 MB file size validation with an inline error message (\"File too large — please select a file under 10 MB\"), base64 encoding of valid selected images, and calls to both `useUpdateProfilePic` and `useSaveCallerUserProfile` on save with a save-error display and proper loading/disabled states.",
  "frontend/src/components/CommentsSheet.tsx": "Bottom sheet for post comments that renders each comment in a `CommentItem` sub-component which fetches the comment author's user data via `useGetUser` and displays their `profilePicData` as a base64 data URL avatar with default fallback.",
  "frontend/src/components/UserListItem.tsx": "User list item component that renders the user's `profilePicData` as a base64 data URL avatar (with default fallback), display name, username, and car info in a clickable row.",
  "frontend/src/pages/HomePage.tsx": "Home feed page that uses the new `useInfiniteFeed` infinite-scroll hook, flattens paginated pages into a single post list, and uses an `IntersectionObserver` on a sentinel element at the bottom to automatically fetch the next 20 posts when the user scrolls near the end, with a spinner during loading and an end-of-feed message when all posts are loaded.",
  "frontend/src/pages/CreateReelPage.tsx": "Create Reel page with file upload (video/image), now enforces a 10 MB size limit (increased from 1.5 MB) with inline error display and toast notifications, shows an updated file size hint, and properly handles submission errors with inline error alerts and re-enabled submit button after failure.",
  "frontend/src/pages/CreateFeedPostPage.tsx": "Create Feed Post page with optional photo upload now enforcing a 10 MB size limit (increased from 1.5 MB), updated hint text, inline error display, image preview with clear button, and proper error handling on submission.",
  "frontend/src/pages/CreateBuildPage.tsx": "Create Build Showcase page now includes an optional photo upload section with a 10 MB file size limit per image, inline error display, image preview with clear button, and proper error handling on submission.",
  "frontend/src/pages/CreateMechanicQuestionPage.tsx": "Create Mechanic Question page now includes an optional photo upload section with a 10 MB file size limit, inline error display, image preview with clear button, and proper error handling on submission.",
  "frontend/src/components/BottomNav.tsx": "Bottom navigation bar with six tabs — Home, Reels, Discover (Compass icon linking to /discover), Create (floating center button), Events, and Profile — with active state highlighting and a CreatePostSheet trigger.",
  "frontend/src/pages/MarketplacePage.tsx": "New Marketplace page displaying all listings in a responsive 2-column grid with category filter chips (All, Parts, Wheels, Audio, Exterior, Interior, Other), a prominent \"LIST ITEM\" button navigating to `/marketplace/create`, seller avatars, condition badges, price display, and an empty state with the marketplace hero image.",
  "frontend/src/pages/CreateListingPage.tsx": "Create Listing page with image upload now enforcing a 10 MB size limit (increased from 1.5 MB), updated hint text, inline error display, and proper error handling on submission.",
  "frontend/src/components/CreatePostSheet.tsx": "Bottom sheet for content creation with five options (Feed Post, Reel, Build Showcase, Mechanic Question, and the new \"List for Sale\" option with amber styling that navigates to `/marketplace/create`).",
  "frontend/src/pages/AboutPage.tsx": "New About RevGrid page with six sections — hero with logo/tagline, platform description, features list with icons, closing movement quote, support section with copyable Cash App handle ($alteredsol), and contact section with Instagram link (@boddysum), all styled with the dark asphalt/amber automotive theme.",
  "frontend/src/components/TopBar.tsx": "Top navigation bar with a new PlayCircle icon button navigating to `/streettube` (highlighted red when active), alongside the existing message, bell, wrench, and info icon buttons, plus updated page title entries for the streettube route.",
  "frontend/src/pages/TunerShopPage.tsx": "Static Altered Imports tuner shop page with a hero banner image, about/mission section, 7 service cards with icons, closing paragraph, prominent tagline, and location/contact cards (Instagram link and tappable phone number), all styled with the dark asphalt/amber automotive theme.",
  "frontend/src/hooks/useLandingMusic.ts": "Custom React hook managing landing page background music — now starts unmuted by default with a fade-in on playback, falls back to muted autoplay if the browser blocks unmuted autoplay, and exposes `isMuted`, `isPlaying`, `toggleMute`, `startPlayback`, and `fadeOutAndStop` controls.",
  "frontend/public/sitemap.xml": "Static XML sitemap conforming to the sitemaps.org schema, listing all 9 publicly accessible RevGrid routes (home, feed, reels, discover, events, marketplace, about, tuner-shop, reels-search) with weekly changefreq and appropriate priority values.",
  "frontend/src/pages/StreetTubePage.tsx": "Fully upgraded self-contained StreetTube page with 10 new features: dark/light theme toggle with localStorage persistence, video comments system with avatar initials and timestamps, channel profile modal with subscribe toggle and video grid, share button that copies a `?v=<videoId>` URL with toast notification, auto-open video from URL query param, Watch Later save/unsave with sidebar entry, Trending view sorted by view count, search results view with horizontal list cards and tag support, notification bell with unread badge and dropdown, video edit/delete for owners with three-dot menu, and enhanced upload flow with thumbnail frame picker slider and comma-separated tags stored as pill badges."
}