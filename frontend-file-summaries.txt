{
  "frontend/src/components/LandingPage.tsx": "Full-screen landing page with the modified import car background image, dark cinematic overlay, RevGrid logo, and a prominent amber-styled Login button that triggers Internet Identity authentication.",
  "frontend/src/components/AuthGate.tsx": "Authentication gate component that shows the LandingPage for unauthenticated users, a profile setup form for new users, and renders children for fully authenticated users with profiles.",
  "frontend/src/components/Layout.tsx": "Authenticated app shell with TopBar, a slim amber-styled aftermarket parts affiliate banner linking to eBay, main content Outlet, and BottomNav. The banner appears on every authenticated page and opens the affiliate link in a new tab.",
  "frontend/index.html": "HTML entry point with updated browser tab title changed from \"CarGram\" to \"RevReel\".",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions, with `useCreatePost` now accepting an optional `mediaData` parameter (typed as `string | null | undefined`) and correctly passing it as the fifth argument to `actor.createPost`, fixing the TypeScript arity error.",
  "frontend/src/pages/ProfilePage.tsx": "Profile page that displays the user's profile picture from `profilePicData` (decoded as a base64 data URL) in the header avatar, with fallback to the default avatar image, alongside all existing tabs, stats, follow/edit actions, and delete functionality.",
  "frontend/src/pages/MessagesPage.tsx": "Messages list page that now fetches the current user's followers and following lists and only displays users with at least a one-way follow relationship as available chat partners, with a \"Followers only\" lock indicator and an empty state prompting users to follow others.",
  "frontend/src/pages/ChatThreadPage.tsx": "Chat thread page that now checks if a follow relationship exists (either direction) before showing the message thread. If no relationship exists, renders a locked/restricted state with a clear message and a Follow button styled in amber; existing conversations between connected users continue to work normally.",
  "frontend/src/components/PostCard.tsx": "Post card component that fetches the author's user data via `useGetUser` and renders their `profilePicData` as a base64 data URL avatar (with default fallback), alongside existing like, comment, and delete functionality.",
  "frontend/src/pages/ReelsPage.tsx": "Vertical snap-scroll reels page that now includes a `parseReelCaption` helper to decode the JSON-encoded caption field (extracting the real caption text and embedded base64 media data), rendering video or image media from either the embedded base64 source or the ExternalBlob URL, with all existing like, comment, delete, author avatar, and search functionality intact.",
  "frontend/src/pages/ReelsSearchPage.tsx": "New dedicated Reels Search page with a debounced search input, 8 category pill filters (Street, Stance, JDM, Drift, Build, Show, Track, Import), a two-column results grid showing reel cards with thumbnails, category badges, author avatars, and captions, plus skeleton loaders and empty/idle states.",
  "frontend/src/App.tsx": "Main app router with all routes registered, now including two new marketplace routes: `/marketplace` rendering `MarketplacePage` and `/marketplace/create` rendering `CreateListingPage`.",
  "frontend/src/pages/DiscoverPage.tsx": "Discover page with user/content search and tabbed grid, now featuring two side-by-side shortcut buttons below the search bar — one for Reels Search and a new one for the Marketplace — both navigating to their respective pages.",
  "frontend/src/components/EditProfileModal.tsx": "Edit profile modal with a circular avatar preview section at the top that shows the current or newly selected profile picture, a hidden file input (accept=\"image/*\") triggered by clicking the avatar or a text button, base64 encoding of the selected image, and calls to both `useUpdateProfilePic` and `useSaveCallerUserProfile` on save.",
  "frontend/src/components/CommentsSheet.tsx": "Bottom sheet for post comments that renders each comment in a `CommentItem` sub-component which fetches the comment author's user data via `useGetUser` and displays their `profilePicData` as a base64 data URL avatar with default fallback.",
  "frontend/src/components/UserListItem.tsx": "User list item component that renders the user's `profilePicData` as a base64 data URL avatar (with default fallback), display name, username, and car info in a clickable row.",
  "frontend/src/pages/HomePage.tsx": "Home feed page that renders all posts using `PostCardWrapper` components; the wrapper no longer passes an `author` prop to `PostCard` since `PostCard` now fetches author data internally.",
  "frontend/src/pages/CreateReelPage.tsx": "Create Reel page with file upload (video/image), now enforces a 10 MB size limit (increased from 1.5 MB) with inline error display and toast notifications, shows an updated file size hint, and properly handles submission errors with inline error alerts and re-enabled submit button after failure.",
  "frontend/src/pages/CreateFeedPostPage.tsx": "Create Feed Post page now includes an optional photo upload section with a 1.5 MB file size guard, inline error display, image preview with clear button, and proper error handling on submission with inline error alerts.",
  "frontend/src/pages/CreateBuildPage.tsx": "Create Build Showcase page with improved error handling — catches submission errors, shows an inline error alert when the mutation fails, and ensures the submit button is re-enabled after failure.",
  "frontend/src/pages/CreateMechanicQuestionPage.tsx": "Create Mechanic Question page with improved error handling — catches submission errors with a descriptive message, shows an inline error alert when the mutation fails, and ensures the submit button is re-enabled after failure.",
  "frontend/src/components/BottomNav.tsx": "Bottom navigation bar with six tabs — Home, Reels, Discover (Compass icon linking to /discover), Create (floating center button), Events, and Profile — with active state highlighting and a CreatePostSheet trigger.",
  "frontend/src/pages/MarketplacePage.tsx": "New Marketplace page displaying all listings in a responsive 2-column grid with category filter chips (All, Parts, Wheels, Audio, Exterior, Interior, Other), a prominent \"LIST ITEM\" button navigating to `/marketplace/create`, seller avatars, condition badges, price display, and an empty state with the marketplace hero image.",
  "frontend/src/pages/CreateListingPage.tsx": "New Create Listing page with a form for title, description, price, condition (radio buttons), category (select dropdown), and image upload (1.5 MB max with preview); calls `useCreateListing` on submit, shows inline errors, disables the button while pending, and navigates to `/marketplace` on success.",
  "frontend/src/components/CreatePostSheet.tsx": "Bottom sheet for content creation with five options (Feed Post, Reel, Build Showcase, Mechanic Question, and the new \"List for Sale\" option with amber styling that navigates to `/marketplace/create`)."
}